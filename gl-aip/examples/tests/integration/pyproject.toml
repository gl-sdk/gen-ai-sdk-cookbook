[project]
name = "tests-integration"
version = "0.1.0"
description = "Integration tests example for GL AIP cookbook"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "glaip-sdk>=0.0.13",
    "pytest>=8.4.2",
]

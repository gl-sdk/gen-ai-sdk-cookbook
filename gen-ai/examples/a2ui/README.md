## ⚙️ Prerequisites

- **Node.js 18+** — <https://nodejs.org/>
- **npm** (comes with Node.js)

## 🚀 Getting Started

1. **Clone the repository & open the directory**

   ```bash
   git clone https://github.com/gl-sdk/gen-ai-sdk-cookbook.git
   cd gen-ai-sdk-cookbook/gen-ai/examples/a2ui
   ```

2. **Install dependencies**

   ```bash
   npm install
   ```

3. **Run the dev server**

   ```bash
   npm run dev
   ```

4. **Open the app**

   Visit <http://localhost:5173> in your browser. You should see a rendered A2UI surface with all supported component types (Text, Button, Card, Row, Column, TextField, CheckBox, Image, Divider, Timeout).

   Interact with the components — button clicks and form inputs will appear in the **Event Log** panel as raw `userAction` payloads.

## 📚 Reference
These examples are based on the [GL SDK Gitbook documentation Getting Started page](https://gdplabs.gitbook.io/glchat/sdk-a2ui/getting-started).
